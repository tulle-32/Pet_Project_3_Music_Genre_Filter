# Как устроен доступ к музыке ВКонтакте

Документ отвечает на вопрос, который выглядит простым: «почему нельзя просто
зайти по ссылке и забрать список?». Ответ определяет всю архитектуру проекта,
поэтому он записан отдельно.

---

## Что происходит при открытии страницы с музыкой

Когда в браузере открывается `vk.ru/audios183275836`, происходит **не один
запрос, а минимум два, и они разные**.

```
  Твой браузер                                        Серверы VK
  (залогинен, cookies есть)

       │  ① GET /audios183275836                          │
       │─────────────────────────────────────────────────▶│
       │                                                  │
       │      HTML-каркас страницы, треков внутри нет     │
       │◀─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
       │                                                  │
       │  ② POST m.vk.ru/audio                            │
       │     act=load_section&offset=0                    │
       │─────────────────────────────────────────────────▶│
       │                                                  │
       │      порция треков: artist · title · duration    │
       │◀─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
       │                                                  │
       │  ③ повтор с offset=100, 200, 300…                │
       │     с паузой около 2 секунд между порциями       │
```

Первый запрос отдаёт пустой каркас. Треки приезжают вторым — на другой адрес,
методом POST, и **только если сервер узнаёт тебя по cookies**. Программа без
сессии получит на шаге ② страницу логина.

Из этого следуют три вещи:

1. **Адрес второго запроса внутренний.** Это не документированный API, у него
   нет обещаний обратной совместимости. Он может измениться в любой момент, и
   всё, что на него завязано, сломается молча.
2. **Нужна сессия.** Приложению придётся хранить у себя ключ от какого-то
   аккаунта, со всеми последствиями по безопасности.
3. **Аудио нам не нужно.** Ссылки на mp3 живут отдельно, они короткоживущие
   и привязаны к сессии. Мы читаем только метаданные — исполнителя, название,
   длительность. Это принципиально другая история и по нагрузке, и по рискам.

---

## Почему официальный путь не работает

**Метод `audio.get`.** До сих пор есть в документации ВК, и там же указано,
что он требует особых прав доступа. Обычным приложениям эти права не выдают
примерно с 2016–2017 годов.

Обидная деталь: у официального API есть поле `genre` с готовым справочником
из 21 жанра (Rock = 1, Metal = 7, Alternative = 21 и так далее). То есть жанр
там был бы бесплатно. Взять его нельзя, поэтому весь пайплайн обогащения
существует именно из-за этого ограничения.

**Авторизация через VK ID.** Даёт стороннему сайту личность пользователя:
имя, фамилию, аватар, почту, телефон, дату рождения, пол. Аудиозаписей в этом
списке нет. Вход через ВК позволяет узнать, *кто* пришёл, но не даёт доступа
к его музыке.

---

## Варианты доступа

| Вариант | Как работает | Оценка |
|---|---|---|
| Официальный API `audio.get` | — | Недоступен |
| Токен технического аккаунта | Приложение ходит на внутренние адреса ВК от имени отдельного аккаунта | Работает; противоречит правилам ВК; риск ограничений на этом аккаунте |
| Расширение для браузера | Каждый пользователь ставит расширение, оно ходит по страницам его собственной сессией | Чисто, масштабируется на многих людей; отдельная разработка на JS |
| Официальная выгрузка архива | Пользователь запрашивает архив своих данных в настройках ВК | Полностью официально; готовится часами; только свои данные |
| Импорт из файла | Список подготовлен любым способом и залит файлом | Всегда работает, используется как основной путь при разработке и в тестах |

---

## Что выбрано и почему

**До релиза:** источник ВК работает от токена отдельного технического
аккаунта, заведённого специально под проект. Причина: приложение должно
делать то, ради чего затевалось, — принять ссылку на чужую страницу и
выкачать список само, не требуя действий от владельца страницы.

**Ограничения, встроенные в код:**

- пауза не меньше 2 секунд между запросами;
- потолок на количество выгрузок в сутки (`VK_DAILY_FETCH_LIMIT`);
- кэширование — повторная выгрузка той же страницы не делает новых запросов;
- аудиофайлы не скачиваются вообще, только метаданные;
- источник читает только те страницы, которые аккаунт и так может открыть.

Смысл этих ограничений не косметический: они делают поведение приложения
неотличимым от человека, листающего страницу, что и есть главный фактор
устойчивости.

**После релиза (v2.0.0):** расширение для браузера, где каждый пользователь
ходит своей сессией. Для человека выглядит так же — вставил ссылку, получил
список, — но риск не концентрируется на одном аккаунте.

**Всегда:** импорт из файла остаётся как запасной путь. Если источник ВК
однажды перестанет работать, проект продолжит работать — просто список
приедет файлом. Ради этого и придуман интерфейс `TrackSource`.

---

## Источники

- [Жанры аудиозаписей — VK Dev](https://dev.vk.ru/ru/reference/objects/audio-genres)
- [VK ID: какие данные получает сторонний сайт](https://id.vk.ru/about/business/go/docs/ru/vkid/latest/oauth-vk)
- [vk_api — модуль работы с аудио, исходный код](https://github.com/python273/vk_api/blob/master/vk_api/audio.py)
- [MusicBrainz API: ограничения по частоте запросов](https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting)
