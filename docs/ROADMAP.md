# Дорожная карта

Каждый шаг — законченный кусок, который можно запустить и потрогать руками.
Каждый заканчивается коммитом и тегом с одинаковым текстом.

Порядок не случайный: сначала то, на чём всё стоит, потом то, ради чего
всё затевалось. Хрупкая часть (источник ВК) появляется в середине — когда
уже есть куда складывать результат и на чём его проверять.

---

## До релиза

| Версия | Что делаем | Что нового изучаем |
|---|---|---|
| **v0.1.0** | **Документация**: архитектура, схема базы, журнал решений, дорожная карта | Проектирование до кода, журнал решений |
| **v0.2.0** | **Фундамент**: модуль Go, `docker-compose` с PostgreSQL, `/health`, линтер | Структура проекта на Go, compose с базой |
| **v0.3.0** | **База и миграции**: двенадцать таблиц, миграции через goose, утилита `music`, справочник жанров с иерархией и его заливка | SQL-схема, версионируемые миграции, встраивание файлов в бинарник |
| **v0.4.0** | **Импорт из файла**: интерфейс `TrackSource`, нормализация строк, дедупликация, журнал импортов, генерация кода запросов через sqlc | Интерфейсы в Go, работа с юникодом, sqlc |
| **v0.5.0** | **Тесты и первый CI**: `go test`, временная база в контейнере, GitHub Actions | pytest-эквивалент в Go, YAML пайплайна |
| **v0.6.0** | **Источник ВК**: вставил ссылку — приложение выкачало список | HTTP-клиент, разбор ответа, ограничитель частоты |
| **v0.7.0** | **Жанры из Last.fm**: интерфейс `GenreProvider`, кэш ответов, справочник тег → жанр | Работа с внешним API, кэширование, секреты |
| **v0.8.0** | **MusicBrainz и уверенность**: второй провайдер, нечёткое сравнение имён, расчёт уверенности, отчёт по нераспознанным тегам | Свой алгоритм сравнения строк, жёсткие лимиты |
| **v0.9.0** | **HTTP API**: `/tracks` с фильтром, `/genres`, `/stats`, карточка трека с разбором «откуда жанр» | Роутинг, пагинация, формы ответов |
| **v1.0.0** | **Релиз**: веб-страница с фильтрами, экспорт в m3u, Dockerfile, сборка образа по тегу, README с инструкцией, бэкапы | CD целиком, многослойный Dockerfile, bash-обвязка |

**Что должен уметь v1.0.0.** Вставил ссылку на страницу с музыкой → приложение
выкачало список → определило жанры → нажал «рок» → получил плейлист, который
открывается в любом плеере.

---

## После релиза

| Версия | Что добавляем | Зачем |
|---|---|---|
| v1.1.0 | Ручные правки жанров (`overrides`) и их применение в фильтре | Алгоритм ошибается, поправку нужно вносить один раз |
| v1.2.0 | Наведение порядка перед публикацией: структурированные логи, обработка ошибок, покрытие тестами, вычитка README, проверка истории на секреты | После этого шага репозиторий можно открывать |
| v1.3.0 | Deezer третьим провайдером, нечёткий поиск через `pg_trgm` | Рост покрытия жанрами, ещё одно расширение PostgreSQL |
| v2.0.0 | Пользователи, вход через VK ID, права доступа к библиотекам, расширение для браузера | Многопользовательский режим по-честному: каждый ходит своей сессией |
| v2.1.0 | Очередь задач, фоновое обогащение, статусы задач | К этому моменту синхронное обогащение уже начнёт мешать — очередь станет решением реальной проблемы |
| v2.2.0 | Метрики Prometheus, дашборд Grafana | Наблюдаемость — второй по востребованности навык в DevOps после CI/CD |
| v3.0.0 | Перенос пайплайна на self-hosted GitLab CI | Осознанное сравнение двух систем, когда механика уже понятна |

---

## Правило выпуска версии

Текст тега, сообщения коммита и записи в `CHANGELOG.md` совпадают:

```
v0.4.0 импорт треков из файла: интерфейс источников, нормализация имён, дедупликация
```

Текст набирается один раз — в `CHANGELOG.md`. Дальше выпуск версии это одна
команда, в которой вводится только номер:

```powershell
.\scripts\release.ps1 v0.4.0
```

Скрипт проверяет формат версии, находит в `CHANGELOG.md` заголовок этой
версии, берёт оттуда текст, делает коммит, ставит аннотированный тег с тем же
текстом и отправляет всё одной командой `git push --follow-tags`. Отправка
тега — это то, что запускает сборку Docker-образа.

Посмотреть, что будет сделано, ничего не меняя:

```powershell
.\scripts\release.ps1 v0.4.0 -DryRun
```
